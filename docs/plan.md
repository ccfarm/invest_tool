# 股东查询工具 — 实施计划

## M0 项目初始化

- [x] 确定仓库结构（`frontend/` + `backend/`）
- [x] 初始化 Vue 3 项目（Vite）与 Python FastAPI 项目
- [x] 配置开发环境与基础规范（代码风格、依赖管理）

## M1 数据采集

- [x] 设计数据模型：股票、股东、持股记录
- [x] 实现爬虫：按股东姓名 / A 股代码抓取持股数据
- [x] 数据清洗与入库
- [x] 定时任务：每小时自动爬取更新
- [x] 爬虫断点续传：记录每只股票抓取状态，中断后自动跳过已完成项
- [x] 全市场抓取与每日增量：`--market` 一次抓全部 A 股，断点续传 + 每日增量更新
- [x] 增量爬虫内置到服务进程：后台线程启动即查一次，之后每 24 小时增量更新
- [x] 网站 PV 统计：今日/累计访问量，页脚展示，持久化到 SQLite
- [x] 微盘股模块：按钮触发筛选、按交易日去重、近 20 次历史下拉、ST/风险黑名单

## M2 后端 API

- [x] 搜索接口：支持股东姓名或 A 股代码，含分页参数
- [x] 返回字段：A股代码、股票名称、股东名称、本期持股、披露时间、较上期变动
- [x] 排序（披露时间从新到旧）与分页
- [x] 错误处理：空输入、无结果
- [x] 接口单元测试

## M3 前端页面

- [x] 搭建 Vue 3 + Vue Router（`/` 搜索页、`/results` 结果页）
- [x] 搜索页：搜索表单 + 空状态引导
- [x] 结果页：持股表格（千分位、变动红/绿/灰徽章）
- [x] 分页交互
- [x] 清除输入、修改关键词重新查询
- [x] 明暗主题 + 响应式布局
- [x] 登录/注册占位（暂不实现鉴权）

## M4 联调与交付

- [x] 前后端联调：搜索 → 结果全流程
- [x] 简单部署方案（如 Docker / 启动脚本）
- [x] 验收测试与文档更新

## M5 趋势向上模块

- [x] 筛选逻辑：MA20 连续 10 个交易日上行（`trend.py`），按换手率升序取 30 只
- [x] 快照存储：`trend_snapshots` 表 + 近 20 次历史下拉回看
- [x] 接口：`/api/trend/refresh`、`/latest`、`/dates`、`/history`、`/kline`
- [x] 定时任务：服务内置，每 6 小时刷新，`ENABLE_SCHEDULED_TREND` 可关闭
- [x] 前端页面：`/trend` 表格 + 代码悬停展示近 3 个月 K 线（含 MA20）
- [x] 单测与冒烟测试：MA20 判定、筛选排序、快照复用、K 线缓存、SEO 快照
